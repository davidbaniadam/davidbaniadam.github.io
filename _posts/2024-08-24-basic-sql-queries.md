---
layout: post
title: "Basic SQL Queries"
date: 2024-08-24 10:00:00 +0000
categories: blog
---

In this post, I will introduce you to some basic SQL queries that you can run on SQL Server.

## SELECT Query

The `SELECT` statement is used to fetch data from a database. Here's an example:

```sql
SELECT * FROM Customers;
```
